package com.example.requestlab.core.designsystem.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight

val AppTypography = Typography(
    // Uses M3 defaults, referencing system sans-serif.
    // Replace FontFamily.Default with a bundled font if brand font is added later.
)

val MonospaceFontFamily = FontFamily.Monospace
